<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chuzzle">

        <activity
            android:name=".controller.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controller.activities.GameActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".controller.activities.SeedActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".controller.activities.OptionsActivity"
            android:label="@string/main_menu_settings" />

    </application>

</manifest>